source 'http://rubygems.org'

# Declare your gem's dependencies in home.gemspec.
# Bundler will treat runtime dependencies like base dependencies, and
# development dependencies will be added by default to the :development group.
gemspec


#gem 'nokogiri'

#gem 'win32-sound', platforms: [:mingw, :mswin]

#gem 'delayed_job_active_record'

#gem 'image_size'

#gem 'ruby-sun-times', require: 'sun_times'

#gem 'devise'

#gem 'closure_tree'
#gem 'awesome_nested_set'

# gem 'websocket-rails' -- not worked in windows, based on redis

# Heroku

=begin

group :production do
  gem 'pg'
  gem 'rails_12factor'
  #ruby "2.2.0"
end

=end

